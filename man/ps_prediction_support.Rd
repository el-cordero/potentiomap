% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support.R
\name{ps_prediction_support}
\alias{ps_prediction_support}
\title{Describe prediction support and extrapolation}
\usage{
ps_prediction_support(
  points,
  surface = NULL,
  template = NULL,
  mask = NULL,
  max_distance = NULL,
  allow_geographic = FALSE
)
}
\arguments{
\item{points}{Training observations as a point \code{SpatVector}, \code{sf} object, or
object readable by \code{terra::vect()}.}

\item{surface}{Optional one-layer prediction \code{SpatRaster}.}

\item{template}{Optional one-layer template when \code{surface} is not supplied.}

\item{mask}{Optional polygon mask used to classify cells.}

\item{max_distance}{Optional positive distance threshold in projected map
units.}

\item{allow_geographic}{Allow longitude/latitude calculations with a classed
warning. The default is \code{FALSE}.}
}
\value{
A \code{potentiomap_support} list containing \code{rasters}, a reason-code
\code{lookup} table, a cell-level \code{records} table, \code{summary}, the standardized
training \code{points}, and \code{call}.
Stable support classes are \code{supported}, \code{outside_training_hull},
\code{beyond_maximum_distance}, \code{outside_mask}, \code{prediction_unavailable}, and
\code{multiple_limitations}.
}
\description{
Classifies raster cells using the training-point convex hull, distance to the
nearest observation, an optional maximum distance, mask membership, and
finite prediction availability. The convex hull is a training-network
geometry, not an aquifer boundary. Predictions are described, not removed.
}
\details{
Distance calculations require projected coordinates by default. An explicit
override reports that longitude/latitude degrees are not linear ground units.
}
\examples{
data("synthetic_wells")
pts <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation",
                      "well_id", "EPSG:26916")
surface <- ps_interpolate(pts, methods = "IDW", grid_res = 200)$IDW
support <- ps_prediction_support(pts, surface = surface,
                                 max_distance = 1000)
support$summary
}
