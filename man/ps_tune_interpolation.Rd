% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning-uncertainty.R
\name{ps_tune_interpolation}
\alias{ps_tune_interpolation}
\title{Tune interpolation parameters under recorded validation partitions}
\usage{
ps_tune_interpolation(
  points,
  method,
  candidates,
  inner_design = "spatial_block",
  outer_design = NULL,
  inner_folds = 5,
  outer_folds = 5,
  repeats = 1,
  metric = "rmse",
  minimum_coverage = 0.9,
  template = NULL,
  mask = NULL,
  grid_res = NULL,
  refit = TRUE,
  seed = 1,
  progress = NULL,
  search = c("grid", "random"),
  maximum_runs = 1000
)
}
\arguments{
\item{points}{Groundwater-head points.}

\item{method}{One of \code{"TPS"}, \code{"IDW"}, \code{"OK"}, or \code{"UK"}.}

\item{candidates}{Candidate table or named parameter list.}

\item{inner_design, outer_design}{Inner and optional outer validation designs.}

\item{inner_folds, outer_folds, repeats}{Fold and repeat counts.}

\item{metric}{Objective metric minimized during selection.}

\item{minimum_coverage}{Minimum finite-prediction coverage.}

\item{template, mask, grid_res}{Fixed mapping controls.}

\item{refit}{Refit the selected configuration to all observations.}

\item{seed}{Deterministic seed.}

\item{progress}{Optional callback.}

\item{search}{Exhaustive grid or reproducible row sampling.}

\item{maximum_runs}{Maximum candidate-by-fold run guard.}
}
\value{
A \code{potentiomap_tuning} object.
}
\description{
Compares explicit candidate configurations using the same deterministic
inner partitions. With an outer design, tuning occurs only inside each outer
training partition and the selected configuration is evaluated on its outer
holdout. Without an outer design, reported performance is tuning performance,
not an unbiased estimate of final predictive performance.
}
\examples{
data("synthetic_wells")
p <- ps_make_points(synthetic_wells[1:12, ], "x", "y", "gw_elevation",
                    "well_id", "EPSG:26916")
tuned <- ps_tune_interpolation(p, "IDW", list(idw_power = c(1.5, 2)),
                               inner_design = "kfold", inner_folds = 3,
                               refit = FALSE, seed = 4)
tuned$candidates[, c("candidate_id", "metric", "coverage", "selected")]
# These are tuning scores, not unbiased final performance estimates.
}
