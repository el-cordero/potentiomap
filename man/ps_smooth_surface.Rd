% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth.R
\name{ps_smooth_surface}
\alias{ps_smooth_surface}
\title{Smooth a potentiometric surface raster}
\usage{
ps_smooth_surface(
  surface,
  window_size = 3,
  method = c("mean", "median"),
  weights = NULL,
  iterations = 1,
  na.rm = TRUE,
  preserve_na = TRUE,
  filename = "",
  overwrite = FALSE
)
}
\arguments{
\item{surface}{A \code{terra::SpatRaster} potentiometric surface.}

\item{window_size}{Odd integer window size used when \code{weights} is \code{NULL}.}

\item{method}{Smoothing statistic. Supported values are \code{"mean"} and
\code{"median"}.}

\item{weights}{Optional odd-dimension numeric matrix of focal weights.
\code{NA} values in the matrix are ignored by \code{terra::focal()}.}

\item{iterations}{Number of smoothing passes.}

\item{na.rm}{Ignore missing values inside the focal window.}

\item{preserve_na}{Preserve the original \code{NA} footprint after smoothing.}

\item{filename}{Optional output raster filename.}

\item{overwrite}{Overwrite \code{filename} when it exists.}
}
\value{
A smoothed \code{terra::SpatRaster}.
}
\description{
Applies a focal moving-window smoother to a potentiometric surface raster.
This can be useful when an interpolated surface is technically valid but too
locally rough for contour development or hydraulic-gradient visualization.
}
\examples{
data("synthetic_wells")
pts <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation",
                      "well_id", "EPSG:26916")
s <- ps_interpolate(pts, grid_res = 100)
smoothed <- ps_smooth_surface(s$TPS, window_size = 5)
smoothed
}
