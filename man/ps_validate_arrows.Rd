% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow.R
\name{ps_validate_arrows}
\alias{ps_validate_arrows}
\title{Validate hydraulic-gradient arrow endpoints}
\usage{
ps_validate_arrows(
  surface,
  arrows,
  tolerance = 1e-06,
  extraction = c("bilinear", "simple")
)
}
\arguments{
\item{surface}{One-layer modeled-head \code{SpatRaster}.}

\item{arrows}{Line \code{SpatVector} or readable vector path.}

\item{tolerance}{Nonnegative head tolerance.}

\item{extraction}{Either \code{"bilinear"} or \code{"simple"} endpoint extraction.}
}
\value{
A \code{potentiomap_arrow_validation} list with validated \code{arrows}, an
arrow-level \code{records} data frame, and a concise \code{summary}.
}
\description{
Samples modeled head at each line base and tip and checks finite raster
support along the line. A downhill pass requires a finite line and a tip no
higher than the base within \code{tolerance}. Geometry is not reversed or bent.
}
\examples{
data("synthetic_wells")
pts <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation",
                      "well_id", "EPSG:26916")
surface <- ps_interpolate(pts, methods = "IDW", grid_res = 150)$IDW
legacy <- ps_flow_arrows(surface, endpoint_action = "none")
checked <- ps_validate_arrows(surface, legacy$arrows)
checked$summary
}
