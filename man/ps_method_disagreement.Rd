% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surface-analysis.R
\name{ps_method_disagreement}
\alias{ps_method_disagreement}
\title{Map disagreement among interpolation methods}
\usage{
ps_method_disagreement(
  surfaces,
  head_measures = c("range", "sd", "mad", "mean_pairwise_absolute"),
  gradient = TRUE,
  min_gradient = 1e-05,
  support = c("intersection", "union"),
  minimum_methods = 2
)
}
\arguments{
\item{surfaces}{Named compatible one-layer head rasters or a
\code{potentiomap_result}.}

\item{head_measures}{Requested head-disagreement measures.}

\item{gradient}{Include gradient-direction disagreement.}

\item{min_gradient}{Gradients below this magnitude are undefined.}

\item{support}{Use only the intersection or allow the union of finite cells.}

\item{minimum_methods}{Minimum finite component count. The default is all
methods for intersection and one for union.}
}
\value{
A \code{potentiomap_disagreement} with disagreement rasters, pairwise and
direction summaries, a flat mask, and method-pair manifest.
}
\description{
Computes head and down-gradient-direction differences among compatible
surfaces. Directed bearings use circular differences from 0 to 180 degrees;
they are not treated as axial orientations.
}
\examples{
r <- terra::rast(nrows = 3, ncols = 3, xmin = 0, xmax = 3, ymin = 0, ymax = 3,
                 crs = "EPSG:26920", vals = 1:9)
d <- ps_method_disagreement(list(a = r, b = r + 1))
d$pairwise_summary
# Head offsets are method differences, not statistical uncertainty.
}
