% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change-network-sensitivity.R
\name{ps_head_change}
\alias{ps_head_change}
\title{Compare paired measurements and modeled head change between events}
\usage{
ps_head_change(
  event_a,
  event_b,
  pair_by,
  event_a_time = NULL,
  event_b_time = NULL,
  method = "TPS",
  template = NULL,
  mask = NULL,
  grid_res = NULL,
  interpolation_control = list(),
  compare_gradient = TRUE
)
}
\arguments{
\item{event_a, event_b}{Point observations for two identified events.}

\item{pair_by}{Well identifier column.}

\item{event_a_time, event_b_time}{Optional explicit event times.}

\item{method}{Interpolation method.}

\item{template, mask, grid_res}{Fixed surface controls.}

\item{interpolation_control}{Named interpolation arguments.}

\item{compare_gradient}{Compare down-gradient directions.}
}
\value{
A \code{potentiomap_head_change} object. Modeled head change is not a
storage, depletion, or volumetric-change estimate.
}
\description{
Compare paired measurements and modeled head change between events
}
\examples{
data("synthetic_events")
a <- subset(synthetic_events, event == "spring")
b <- subset(synthetic_events, event == "autumn")
pa <- ps_make_points(a, "x", "y", "head", "well_id", "EPSG:26916")
pb <- ps_make_points(b, "x", "y", "head", "well_id", "EPSG:26916")
change <- ps_head_change(pa, pb, "well_id", method = "IDW", grid_res = 300)
change$summary
# Modeled head change is not a storage-change estimate.
}
