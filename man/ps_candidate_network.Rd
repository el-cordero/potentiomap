% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change-network-sensitivity.R
\name{ps_candidate_network}
\alias{ps_candidate_network}
\title{Rank explicit candidate monitoring locations with recorded constraints}
\usage{
ps_candidate_network(
  existing_points,
  candidates,
  objective = c("spatial_coverage", "support_gap", "kriging_variance_reduction",
    "user_score"),
  n_select = 1,
  target = NULL,
  variogram_model = NULL,
  trend = NULL,
  minimum_existing_distance = 0,
  minimum_candidate_distance = 0,
  allowed_area = NULL,
  exclusion_area = NULL,
  cost = NULL,
  user_score = NULL,
  sequential = TRUE
)
}
\arguments{
\item{existing_points}{Existing monitoring points.}

\item{candidates}{Explicit candidate point locations.}

\item{objective}{Candidate-scoring objective.}

\item{n_select}{Number selected by sequential greedy ranking.}

\item{target}{Explicit target points or raster for target-weighted objectives.}

\item{variogram_model, trend}{Model for kriging-variance reduction.}

\item{minimum_existing_distance, minimum_candidate_distance}{Spacing rules.}

\item{allowed_area, exclusion_area}{Spatial constraints.}

\item{cost}{Optional finite positive candidate costs.}

\item{user_score}{Optional supplied scores.}

\item{sequential}{Update scores after each choice.}
}
\value{
A \code{potentiomap_candidate_network} object. The greedy sequence is not
claimed to be globally optimal or to identify drillable sites.
}
\description{
Rank explicit candidate monitoring locations with recorded constraints
}
\examples{
data("synthetic_wells", "synthetic_candidate_sites")
p <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation",
                    "well_id", "EPSG:26916")
c <- terra::vect(subset(synthetic_candidate_sites, !excluded),
                 geom = c("x", "y"), crs = "EPSG:26916")
design <- ps_candidate_network(p, c, n_select = 2,
                               objective = "spatial_coverage")
design$selected_sequence
# Sequential greedy selection is not a globally optimal drilling plan.
}
