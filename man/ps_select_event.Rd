% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observations-expansion.R
\name{ps_select_event}
\alias{ps_select_event}
\title{Select a groundwater monitoring event}
\usage{
ps_select_event(
  data,
  id,
  datetime,
  center,
  window,
  rule = c("nearest", "earliest", "latest", "best_quality"),
  quality = NULL,
  timezone = "UTC",
  maximum_span = NULL,
  maximum_span_action = c("warn", "error")
)
}
\arguments{
\item{data}{Observation data frame.}

\item{id, datetime}{Column names for well ID and measurement time.}

\item{center}{Target time coercible to \code{POSIXct}.}

\item{window}{Nonnegative seconds, a \code{difftime}, or a string accepted by
\code{as.difftime()}.}

\item{rule}{Deterministic selection rule.}

\item{quality}{Optional quality column for \code{best_quality}; lower sorted value
is preferred and time distance breaks ties.}

\item{timezone}{Time zone used to parse and retain times.}

\item{maximum_span}{Optional maximum selected-event span.}

\item{maximum_span_action}{Warn or error when the maximum is exceeded.}
}
\value{
A \code{potentiomap_event_selection} containing selected, excluded and tie
records plus an event summary.
}
\description{
Selects at most one record per well inside a symmetric time window. The
result records the actual measurement span; being inside a window does not
by itself make an event synoptic and repeated values are never averaged.
}
\examples{
d <- data.frame(well = c("A", "A", "B"),
  time = c("2026-01-01 00:00", "2026-01-01 02:00", "2026-01-01 01:00"))
ev <- ps_select_event(d, "well", "time", "2026-01-01 01:00", 3 * 3600)
ev$selected
# The recorded span still requires a study-specific synoptic judgment.
}
