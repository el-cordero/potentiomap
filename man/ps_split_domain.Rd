% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regions-profiles.R
\name{ps_split_domain}
\alias{ps_split_domain}
\title{Validate and split an explicit hydrogeologic domain}
\usage{
ps_split_domain(
  domain,
  regions,
  region_id,
  points = NULL,
  overlap_action = c("error", "priority"),
  gap_action = c("report", "error"),
  boundary_action = c("error", "assign_by_priority", "duplicate")
)
}
\arguments{
\item{domain}{Domain polygon.}

\item{regions}{Explicit region polygons.}

\item{region_id}{Unique region identifier field.}

\item{points}{Optional monitoring points to assign.}

\item{overlap_action}{Error or preserve priority order.}

\item{gap_action}{Report or error for domain gaps.}

\item{boundary_action}{Error, priority assignment, or duplicate assignments.}
}
\value{
A \code{potentiomap_domain_split} object. Regions are never inferred from
monitoring points and invalid geometry is never silently repaired.
}
\description{
Validate and split an explicit hydrogeologic domain
}
\examples{
data("synthetic_regions")
regions <- terra::vect(synthetic_regions, geom = "wkt", crs = "EPSG:26916")
domain <- terra::as.polygons(terra::ext(500000, 503000, 4640000, 4642500),
                             crs = "EPSG:26916")
split <- ps_split_domain(domain, regions, "region_id")
split$summary
# Explicit regions are not inferred groundwater-flow boundaries.
}
