% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surface-analysis.R
\name{ps_vertical_gradient}
\alias{ps_vertical_gradient}
\title{Calculate a vertical hydraulic gradient}
\usage{
ps_vertical_gradient(
  upper_head,
  lower_head,
  upper_elevation,
  lower_elevation,
  positive = c("upward", "downward"),
  tolerance = 0,
  align = c("error", "to_upper", "to_lower", "template"),
  template = NULL,
  event_metadata = NULL
)
}
\arguments{
\item{upper_head, lower_head}{Numeric paired heads or one-layer rasters.}

\item{upper_elevation, lower_elevation}{Matching absolute elevations, not
unidentified depths.}

\item{positive}{Sign convention.}

\item{tolerance}{Nonnegative near-zero gradient tolerance.}

\item{align, template}{Explicit raster alignment controls.}

\item{event_metadata}{Optional list documenting compatible event, datum,
units, interval and screen-midpoint assumptions.}
}
\value{
A \code{potentiomap_vertical_gradient} with component products, direction
class, support, and sign convention. No flux field is returned.
}
\description{
Uses absolute upper and lower observation elevations. With an upward-positive
convention, the primary gradient is \code{(lower_head - upper_head) / (upper_elevation - lower_elevation)}. The result indicates potential vertical
direction and is not vertical groundwater flux.
}
\examples{
vg <- ps_vertical_gradient(10, 12, 100, 90)
vg$gradient
# The positive gradient indicates upward driving potential, not flux.
}
