% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-expansion.R
\name{ps_validate}
\alias{ps_validate}
\title{Validate potentiometric-surface interpolation}
\usage{
ps_validate(
  points,
  methods = c("TPS", "IDW", "OK", "UK"),
  design = c("loocv", "kfold", "spatial_block", "leave_cluster_out", "user_folds",
    "independent"),
  validation_points = NULL,
  fold_id = NULL,
  cluster = NULL,
  folds = 5,
  repeats = 1,
  block_size = NULL,
  template = NULL,
  mask = NULL,
  grid_res = NULL,
  domain_policy = c("fixed", "training"),
  prediction_mode = c("raster", "direct"),
  metrics = c("me", "mae", "rmse", "medae", "maxae"),
  support = TRUE,
  sampling_weight = NULL,
  interpolation_control = list(),
  seed = 1,
  progress = NULL
)
}
\arguments{
\item{points}{Training groundwater-head points with stable IDs.}

\item{methods}{Interpolation methods.}

\item{design}{Validation design.}

\item{validation_points}{Independent compatible validation points.}

\item{fold_id, cluster}{Assignment vector or column.}

\item{folds, repeats}{Positive counts.}

\item{block_size}{Spatial-block size in projected units.}

\item{template, mask, grid_res}{Fixed mapping geometry controls.}

\item{domain_policy}{Fixed or fold-training-derived raster domain.}

\item{prediction_mode}{Full raster/extraction sequence or supported direct
prediction.}

\item{metrics}{Error metrics; residual is predicted minus observed.}

\item{support}{Calculate held-out hull/distance/mask support.}

\item{sampling_weight}{Optional weights for explicitly independent probability
validation records only.}

\item{interpolation_control}{Named arguments passed to interpolation.}

\item{seed}{Deterministic master seed.}

\item{progress}{Optional callback \verb{(index, total, run_id, status)}.}
}
\value{
A \code{potentiomap_validation} with predictions, metrics, fold/partition/
fit manifests, support summary, settings and captured conditions.
}
\description{
Evaluates requested methods under an explicit leave-one-out, k-fold, spatial
block, cluster, user-fold, or independent validation prediction task.
Held-out heads never construct folds and held-out records never enter their
training fit. Cross-validation performance is not automatically area-wide map
accuracy.
}
\examples{
data("synthetic_wells")
pts <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation",
                      "well_id", "EPSG:26916")
val <- ps_validate(pts, methods = "IDW", design = "kfold", folds = 3,
                   prediction_mode = "direct", seed = 7)
val$metrics
# These scores describe the stated folds, not design-unbiased map accuracy.
}
\references{
Roberts et al. (2017), \doi{10.1111/ecog.02881}; Wadoux et al.
(2021), \doi{10.1016/j.ecolmodel.2021.109692}.
}
