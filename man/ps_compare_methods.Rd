% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-expansion.R
\name{ps_compare_methods}
\alias{ps_compare_methods}
\title{Compare validated interpolation methods}
\usage{
ps_compare_methods(
  validation,
  metric = "rmse",
  design = NULL,
  support_subset = c("all", "finite", "supported"),
  minimum_coverage = 0.9,
  tie_tolerance = NULL,
  objective_weights = NULL,
  select = FALSE
)
}
\arguments{
\item{validation}{A \code{potentiomap_validation} or documented compatible metric
table.}

\item{metric}{One or more metric columns.}

\item{design}{One validation design; required when multiple designs exist.}

\item{support_subset}{All scheduled, finite, or supported predictions.}

\item{minimum_coverage}{Minimum finite coverage.}

\item{tie_tolerance}{Nonnegative absolute metric tolerance; default is a
scale-aware square-root machine tolerance.}

\item{objective_weights}{Explicit named nonnegative weights for multiple
criteria.}

\item{select}{Select one method only after all selection gates pass.}
}
\value{
A \code{potentiomap_method_comparison} with ranking, Pareto status and
optional selection.
}
\description{
Ranks adequately covered methods within an explicit validation design and
support subset. It does not declare a universally best method and does not
aggregate multiple objectives unless the user supplies weights.
}
\examples{
data("synthetic_wells")
pts <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation", "well_id", "EPSG:26916")
val <- ps_validate(pts, "IDW", "kfold", folds = 3, prediction_mode = "direct")
ps_compare_methods(val)$ranking
# Ranking is specific to this design and objective.
}
