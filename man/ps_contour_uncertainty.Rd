% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning-uncertainty.R
\name{ps_contour_uncertainty}
\alias{ps_contour_uncertainty}
\title{Construct pointwise contour-uncertainty bands}
\usage{
ps_contour_uncertainty(
  uncertainty,
  levels,
  probability = 0.9,
  method = c("empirical_crossing", "gaussian_pointwise"),
  keep_realized_contours = FALSE,
  minimum_realizations = 20,
  accept_gaussian = FALSE
)
}
\arguments{
\item{uncertainty}{A \code{potentiomap_uncertainty} object.}

\item{levels}{Contour head levels.}

\item{probability}{Central pointwise probability.}

\item{method}{Empirical realization crossing or Gaussian pointwise method.}

\item{keep_realized_contours}{Retain contours for each realization.}

\item{minimum_realizations}{Minimum successful empirical realizations.}

\item{accept_gaussian}{Explicitly accept the Gaussian pointwise assumption.}
}
\value{
A \code{potentiomap_contour_uncertainty} object. Bands are pointwise, not
simultaneous confidence regions.
}
\description{
Construct pointwise contour-uncertainty bands
}
\examples{
data("synthetic_wells")
p <- ps_make_points(synthetic_wells[1:14, ], "x", "y", "gw_elevation",
                    "well_id", "EPSG:26916")
fit <- suppressWarnings(ps_interpolate(p, methods = "OK", grid_res = 300,
                                       return = "result"))
u <- ps_surface_uncertainty(fit, approach = "kriging_variance")
cu <- ps_contour_uncertainty(u, levels = 168, method = "gaussian_pointwise",
                             accept_gaussian = TRUE)
cu$level_manifest
# This is a pointwise band, not a simultaneous confidence region.
}
