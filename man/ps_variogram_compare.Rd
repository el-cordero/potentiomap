% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variograms-expansion.R
\name{ps_variogram_compare}
\alias{ps_variogram_compare}
\title{Compare fitted variogram models}
\usage{
ps_variogram_compare(
  variogram,
  models = c("Sph", "Exp", "Gau", "Mat"),
  initial = NULL,
  fit_method = 7,
  anisotropy = NULL,
  validation = NULL,
  select = FALSE,
  selection_metric = c("validation_rmse", "weighted_sse")
)
}
\arguments{
\item{variogram}{A \code{potentiomap_variogram} or gstat empirical variogram.}

\item{models}{Candidate model abbreviations.}

\item{initial}{Optional model or named model-specific initial values.}

\item{fit_method}{gstat fit method.}

\item{anisotropy}{Optional explicit anisotropy result or \code{c(angle, ratio)}.}

\item{validation}{Optional compatible validation result/table.}

\item{select}{Select a model using an explicitly supplied criterion.}

\item{selection_metric}{Validation RMSE or weighted variogram SSE.}
}
\value{
A \code{potentiomap_variogram_comparison} with every fit and ranking.
}
\description{
Fits each requested gstat variogram candidate independently and preserves
initial values, fitted parameters, singular status, weighted fitting error,
warnings, and errors. Smallest variogram SSE is not proof of best predictive
performance.
}
\examples{
data("synthetic_wells")
pts <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation",
                      "well_id", "EPSG:26916")
cmp <- ps_variogram_compare(ps_variogram(pts), c("Sph", "Exp"))
cmp$ranking
# Weighted SSE alone is not a predictive-performance guarantee.
}
