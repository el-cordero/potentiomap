% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observations-expansion.R
\name{ps_check_observations}
\alias{ps_check_observations}
\title{Check groundwater observation records}
\usage{
ps_check_observations(
  data,
  x = NULL,
  y = NULL,
  value = NULL,
  id = NULL,
  datetime = NULL,
  unit = NULL,
  vertical_datum = NULL,
  depth = NULL,
  surface_elevation = NULL,
  screen_top = NULL,
  screen_bottom = NULL,
  unit_group = NULL,
  duplicate_tolerance = 0,
  action = c("report", "return_clean")
)
}
\arguments{
\item{data}{A data frame, \code{sf} object, or point \code{SpatVector}.}

\item{x, y, value, id, datetime, unit, vertical_datum, depth, surface_elevation}{Optional column names for location, measurement, timing, unit, datum, and
land-surface fields.}

\item{screen_top, screen_bottom, unit_group}{Optional screen-elevation and
hydrogeologic-unit column names.}

\item{duplicate_tolerance}{Nonnegative coordinate distance within which
different records are flagged as colocated. Use projected map units.}

\item{action}{Return only the report or also deterministically remove records
with missing IDs, coordinates, or heads.}
}
\value{
A \code{potentiomap_observation_check} with \code{issues}, original \code{data},
\code{retained}, \code{removed}, counts, settings, metadata, and conditions.
}
\description{
Reports deterministic input conflicts and statistical review flags before
interpolation. Unusual values are never automatically treated as errors or
deleted. Coordinate distance checks require a known projected CRS; longitude
and latitude are flagged for planar analysis. Screen and head comparisons
assume all elevations use one documented vertical datum.
}
\examples{
d <- data.frame(id = c("A", "A"), x = c(0, 0), y = c(0, 0),
                head = c(10, 11))
attr(d, "crs") <- "EPSG:26920"
check <- ps_check_observations(d, "x", "y", "head", "id")
check$issues
# Flags require hydrogeologic review; they do not prove a record is wrong.
}
