% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observations-expansion.R
\name{ps_screen_groups}
\alias{ps_screen_groups}
\title{Assign or validate monitoring-well screen groups}
\usage{
ps_screen_groups(
  data,
  mode = c("existing", "rules", "depth_bins"),
  unit_col = NULL,
  screen_top,
  screen_bottom,
  rules = NULL,
  breaks = NULL,
  labels = NULL,
  overlap_required = 0.5,
  ambiguous_action = c("report", "error")
)
}
\arguments{
\item{data}{Observation data frame.}

\item{mode}{Existing labels, explicit interval rules, or descriptive bins.}

\item{unit_col}{Existing-label column.}

\item{screen_top, screen_bottom}{Screen-elevation column names; top must be
greater than bottom.}

\item{rules}{Data frame with \code{unit}, \code{top}, and \code{bottom} absolute elevations.}

\item{breaks, labels}{Bin specification for \code{depth_bins}.}

\item{overlap_required}{Minimum fraction of screen length overlapping a rule.}

\item{ambiguous_action}{Return or error on multiple qualifying groups.}
}
\value{
A \code{potentiomap_screen_groups} with assignments, overlap records,
ambiguous/unclassified records, rules and summary.
}
\description{
Preserves an existing water-bearing-unit field, applies explicit interval
rules, or creates descriptive depth/elevation bins. Screen intervals alone do
not establish hydrostratigraphic identity, and depth bins are never labeled as
aquifers.
}
\examples{
d <- data.frame(well = c("A", "B"), top = c(10, 5), bottom = c(8, 1))
rules <- data.frame(unit = c("shallow", "deep"), top = c(12, 6), bottom = c(6, 0))
g <- ps_screen_groups(d, "rules", screen_top = "top", screen_bottom = "bottom", rules = rules)
g$assigned
# Rule assignments remain conditional on user-supplied hydrogeologic rules.
}
