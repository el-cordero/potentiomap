% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouped.R
\name{ps_interpolate_grouped}
\alias{ps_interpolate_grouped}
\title{Interpolate grouped groundwater observations}
\usage{
ps_interpolate_grouped(
  data,
  group_cols,
  value = "Z",
  x = "x",
  y = "y",
  name_col = NULL,
  crs = NULL,
  template_mode = c("shared", "group"),
  mask = NULL,
  mask_mode = c("shared", "group"),
  output_dir = NULL,
  progress = NULL,
  ...
)
}
\arguments{
\item{data}{Observation data accepted by \code{\link[=ps_make_points]{ps_make_points()}}.}

\item{group_cols}{One or more explicit grouping columns.}

\item{value, x, y, name_col, crs}{Point-preparation arguments.}

\item{template_mode}{\code{"shared"} for one output grid or \code{"group"} for a grid
derived independently within each group.}

\item{mask}{Optional shared mask, or a named list when \code{mask_mode = "group"}.}

\item{mask_mode}{\code{"shared"} or \code{"group"}.}

\item{output_dir}{Optional directory for group exports. No files are written
when \code{NULL}.}

\item{progress}{Optional function called as
\code{progress(index, total, group_id, status)}.}

\item{...}{Arguments passed to \code{\link[=ps_interpolate]{ps_interpolate()}}. Structured results are
always retained by group.}
}
\value{
A \code{potentiomap_grouped_result} containing \code{results}, a deterministic
\code{manifest}, \code{group_keys}, captured \code{conditions}, and the original \code{call}.
Empty and failed groups remain in the manifest.
}
\description{
Runs separate analyses for explicit event, aquifer, water-bearing-unit,
season, or other grouping columns. Training records are never combined across
groups. A shared template controls only output geometry; it does not share
observations or fit information.
}
\examples{
data("synthetic_wells")
grouped <- transform(
  synthetic_wells,
  event = rep(c("spring", "autumn"), each = 16),
  unit = rep(c("upper", "lower"), times = 16)
)
result <- ps_interpolate_grouped(
  grouped, c("event", "unit"), value = "gw_elevation",
  name_col = "well_id", crs = "EPSG:26916",
  methods = "IDW", grid_res = 300
)
result$manifest
}
