% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variograms-expansion.R
\name{ps_anisotropy}
\alias{ps_anisotropy}
\title{Explore directional anisotropy in hydraulic head}
\usage{
ps_anisotropy(
  points,
  formula = Z ~ 1,
  directions = seq(0, 135, by = 45),
  tolerance = 22.5,
  cutoff = NULL,
  width = NULL,
  model = "Sph",
  minimum_pairs = 20,
  validation = FALSE
)
}
\arguments{
\item{points}{Groundwater-head points.}

\item{formula}{Variogram trend formula.}

\item{directions}{Direction angles.}

\item{tolerance}{Direction tolerance in degrees.}

\item{cutoff, width}{Variogram controls.}

\item{model}{Candidate directional model.}

\item{minimum_pairs}{Minimum total pairs required for a directional fit.}

\item{validation}{Optionally request a documented isotropic/anisotropic
validation comparison (recorded as not run when no validation design is
supplied).}
}
\value{
A \code{potentiomap_anisotropy} with empirical values, directional fits,
major direction, minor-to-major range ratio, and conditions.
}
\description{
Calculates directional variograms and exploratory fitted ranges. The major
continuity direction is periodic over 180 degrees and uses gstat's
clockwise-from-North convention. Weak evidence is warned and anisotropy is
never activated in interpolation unless supplied explicitly.
}
\examples{
data("synthetic_wells")
pts <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation",
                      "well_id", "EPSG:26916")
a <- ps_anisotropy(pts, minimum_pairs = 5)
a$summary
# This exploratory result is not applied automatically.
}
