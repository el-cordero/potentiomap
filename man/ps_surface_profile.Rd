% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regions-profiles.R
\name{ps_surface_profile}
\alias{ps_surface_profile}
\title{Extract one or more surface profiles along explicit lines}
\usage{
ps_surface_profile(
  lines,
  surfaces,
  step = NULL,
  n = NULL,
  support = NULL,
  distance_method = c("projected", "geodesic")
)
}
\arguments{
\item{lines}{One or more line features.}

\item{surfaces}{Named compatible surfaces or an interpolation result.}

\item{step, n}{Explicit spacing or count per line.}

\item{support}{Optional support/uncertainty rasters to extract.}

\item{distance_method}{Projected or explicit geodesic distance.}
}
\value{
A \code{potentiomap_profile} with monotonically increasing chainage.
}
\description{
Extract one or more surface profiles along explicit lines
}
\examples{
data("synthetic_wells", "synthetic_transect")
p <- ps_make_points(synthetic_wells, "x", "y", "gw_elevation",
                    "well_id", "EPSG:26916")
s <- ps_interpolate(p, methods = "IDW", grid_res = 250)$IDW
line <- terra::vect(synthetic_transect, geom = "wkt", crs = "EPSG:26916")
profile <- ps_surface_profile(line, list(head = s), n = 12)
head(profile$profile)
# Unsupported raster sections remain missing rather than being invented.
}
