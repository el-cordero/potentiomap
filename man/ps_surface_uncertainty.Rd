% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning-uncertainty.R
\name{ps_surface_uncertainty}
\alias{ps_surface_uncertainty}
\title{Quantify model-conditional or resampling surface variability}
\usage{
ps_surface_uncertainty(
  x = NULL,
  points = NULL,
  method = NULL,
  approach = c("kriging_variance", "conditional_simulation", "tps_standard_error",
    "resampling_sensitivity"),
  nsim = 100,
  probabilities = c(0.05, 0.5, 0.95),
  resampling_design = NULL,
  template = NULL,
  mask = NULL,
  keep_realizations = FALSE,
  output_directory = NULL,
  seed = 1,
  progress = NULL,
  exceedance_levels = NULL
)
}
\arguments{
\item{x}{A structured interpolation result.}

\item{points}{Points used for resampling sensitivity when \code{x} is absent.}

\item{method}{Interpolation method for resampling.}

\item{approach}{Uncertainty or sensitivity approach.}

\item{nsim}{Number of simulations or resamples.}

\item{probabilities}{Pointwise quantile probabilities.}

\item{resampling_design}{\code{"case"}, \code{"jackknife"}, or a list with a
\code{type} and spatial \code{group} vector.}

\item{template, mask}{Mapping geometry controls.}

\item{keep_realizations}{Retain realization rasters in memory.}

\item{output_directory}{Optional realization directory.}

\item{seed}{Deterministic seed.}

\item{progress}{Optional callback.}

\item{exceedance_levels}{Optional head levels for exceedance probability.}
}
\value{
A \code{potentiomap_uncertainty} object. Resampling products are
sensitivity summaries, not formal confidence intervals.
}
\description{
Quantify model-conditional or resampling surface variability
}
\examples{
data("synthetic_wells")
p <- ps_make_points(synthetic_wells[1:14, ], "x", "y", "gw_elevation",
                    "well_id", "EPSG:26916")
fit <- suppressWarnings(ps_interpolate(p, methods = "OK", grid_res = 250,
                                       return = "result"))
uncertainty <- ps_surface_uncertainty(fit, approach = "kriging_variance")
uncertainty$method_manifest
# Kriging variance is conditional on the retained covariance model.
}
