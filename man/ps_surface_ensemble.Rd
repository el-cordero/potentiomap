% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surface-analysis.R
\name{ps_surface_ensemble}
\alias{ps_surface_ensemble}
\title{Combine compatible potentiometric surfaces}
\usage{
ps_surface_ensemble(
  surfaces,
  statistic = c("mean", "median", "weighted_mean", "quantile"),
  weights = NULL,
  probabilities = c(0.1, 0.5, 0.9),
  support = c("intersection", "union"),
  minimum_methods = NULL,
  method_metadata = NULL
)
}
\arguments{
\item{surfaces}{Named compatible one-layer head rasters or a
\code{potentiomap_result}.}

\item{statistic}{Cellwise mean, median, named weighted mean, or quantiles.}

\item{weights}{Named nonnegative weights for \code{weighted_mean}. Attribute
\code{origin} may record how they were derived.}

\item{probabilities}{Quantile probabilities.}

\item{support}{Use only the intersection or allow the union of finite cells.}

\item{minimum_methods}{Minimum finite component count. The default is all
methods for intersection and one for union.}

\item{method_metadata}{Optional named metadata list supplementing raster
metadata.}
}
\value{
A \code{potentiomap_ensemble} with ensemble, count, extrema, SD, MAD and
method/support manifests.
}
\description{
Calculates a cellwise ensemble and method-spread layers from compatible head
surfaces. Method spread is disagreement among supplied surfaces, not
statistical uncertainty, and an ensemble is not automatically more accurate
than a component method.
}
\examples{
r <- terra::rast(nrows = 2, ncols = 2, xmin = 0, xmax = 2, ymin = 0, ymax = 2,
                 crs = "EPSG:26920", vals = 1:4)
e <- ps_surface_ensemble(list(a = r, b = r + 2))
e$ensemble
# The spread records method disagreement, not a confidence interval.
}
